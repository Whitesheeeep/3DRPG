namespace RPG.DialogueSystemModule
{
    #region Condition 与 Action 定义

    // DialogueCondition、DialogueAction 与 DialogueCommandContext 定义在 DialogueRuntime，
    // 让运行结果、上下文和命令基类保持同一套运行时契约。

    #endregion
}
