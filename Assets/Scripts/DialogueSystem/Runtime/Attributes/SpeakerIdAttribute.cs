using UnityEngine;

namespace RPG.DialogueSystem
{
    /// <summary>
    /// 标记需要从 DialogueSpeakerIdSettings 严格选择的 SpeakerId 字符串字段。
    /// </summary>
    public sealed class SpeakerIdAttribute : PropertyAttribute
    {
    }
}
