namespace RPG.SkillSystem.Editor
{
    public static class SKillConfigDoubleClickHelper
    {
        private static void OpenSkillConfigInEditor(SkillConfig config)
        {
            if (config == null) return;

            // 打开 SkillConfig 编辑器窗口

        }
    }
}