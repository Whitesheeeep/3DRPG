using System;
using System.Collections;
using System.Collections.Generic;
using Animancer;
using UnityEngine;

public class AnimancerMixerLearn : MonoBehaviour
{
    private AnimancerComponent animaner;

    private void Awake()
    {
        animaner = GetComponent<AnimancerComponent>();
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
