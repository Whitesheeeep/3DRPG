namespace RPG.Character.Animation
{
    /// <summary>
    /// 定义角色 Animancer 的固定语义层；枚举数值同时是不可变的 Animancer Layer Index。
    /// </summary>
    public enum AnimationLayerType
    {
        Base = 0,
        Action = 1,
        UpperBody = 2,
        Additive = 3
    }
}
